15 June 2006

probably don't delete this unless the sites to which it refers are both gone

The Javascript tabifier site may send traffic this way, so such traffic is being logged in ~/pub/safe/general_log.txt